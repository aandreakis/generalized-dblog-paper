\* Invariant run: the trusted point splice (Proposition 6's shape) on
\* a small configuration.  The dump is atomic at its true coordinate,
\* the point bracket sits at that coordinate, and consumption follows.
\* Expected verdict: every invariant holds, including the trajectory
\* from the splice coordinate onward (Corollary 1 at the instance).
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 3
  Keys <- MainKeys
  Vals <- MainVals
  BracketMode = "trusted"
INVARIANTS
  TypeOK
  FrontierExact
  SpliceTrajectory
CHECK_DEADLOCK FALSE
