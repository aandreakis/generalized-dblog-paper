\* Proposition 1, parts (2) and (3), on the countermodel configuration:
\* one chunk over two keys, up to two data events.  The contract's
\* invariants hold here even though reads can tear across the bracket.
\* Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  Keys <- TornKeys
  Vals <- TornVals
  ChunkDoms <- TornChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
  ContinuationExact
  PerKeyMonotone
CHECK_DEADLOCK FALSE
