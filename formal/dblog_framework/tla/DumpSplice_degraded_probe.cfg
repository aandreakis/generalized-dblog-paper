\* PROBE RUN, the low-edge overclaim excluded by Theorem 8.  Same degraded widening as
\* DumpSplice_degraded.cfg, additionally asserting the trajectory
\* claim from inside the widened bracket.  EXPECTED TO FAIL: the dump
\* verdicts are honest at the true coordinate only, so an intermediate
\* replay below it can already show the dump's newer values.  T2 and
\* the safe canonical trajectory from the high edge remain.  TLC does
\* not model whether the latent true coordinate is operationally known
\* or exhibited, so this probe makes no claim about that evidence.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  Keys <- MainKeys
  Vals <- MainVals
  BracketMode = "degraded"
INVARIANTS
  TypeOK
  WidenedTrajectory
CHECK_DEADLOCK FALSE
