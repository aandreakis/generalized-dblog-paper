\* Invariant run: the watermark loop on a small, non-trivial
\* configuration (3 keys in 2 chunks, up to 4 data events, deletes
\* included).  Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 4
  Keys <- MainKeys
  Vals <- MainVals
  ChunkDoms <- MainChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
  ContinuationExact
  PerKeyMonotone
CHECK_DEADLOCK FALSE
