\* MUTATION RUN, Proposition 1, part (1).  Same configuration as
\* WatermarkLoop_torn_green.cfg, with the additional assertion that a
\* single shared in-bracket read instant exists for every completed
\* chunk.  O2 deliberately does not require one, so this assertion is
\* EXPECTED TO FAIL: TLC's counterexample is the torn read of
\* Proposition 1 (one key read before an event committed, the other
\* read after a later event, no single position honest for both).
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  Keys <- TornKeys
  Vals <- TornVals
  ChunkDoms <- TornChunkDoms
INVARIANTS
  TypeOK
  SharedInstantExists
CHECK_DEADLOCK FALSE
