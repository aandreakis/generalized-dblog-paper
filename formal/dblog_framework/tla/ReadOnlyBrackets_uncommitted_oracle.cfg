\* DELIBERATE OUT-OF-CONTRACT MUTATION. UncommittedProbe = TRUE permits
\* a low executed-set sample after the first visible event of a transaction
\* whose remainder is still pending. EXPECTED TO FAIL: OracleFaithful.
\* This probes the consequence of violating S-LOG; it does not relax the
\* committed-only theory or any lawful configuration.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 2
  BoundarySampling = TRUE
  UncommittedProbe = TRUE
  Keys <- AlinKeys
  Vals <- AlinVals
  ChunkDoms <- AlinChunkDoms
INVARIANTS
  TypeOK
  OracleFaithful
CHECK_DEADLOCK FALSE
