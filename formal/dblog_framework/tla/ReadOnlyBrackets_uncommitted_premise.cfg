\* DELIBERATE OUT-OF-CONTRACT MUTATION. UncommittedProbe = TRUE permits
\* one transaction to become visible in two pieces. EXPECTED TO FAIL:
\* CommittedOnly, the executable form of the paper's committed-log
\* premise. This configuration is a negative probe, never a DBLog run.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 2
  BoundarySampling = TRUE
  UncommittedProbe = TRUE
  Keys <- AlinKeys
  Vals <- AlinVals
  ChunkDoms <- AlinChunkDoms
INVARIANTS
  TypeOK
  CommittedOnly
CHECK_DEADLOCK FALSE
