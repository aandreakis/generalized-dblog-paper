\* MUTATION RUN, Theorem 8's guard.  A wrong asserted point is trusted
\* with a degenerate point bracket, which the degradation theorem
\* forbids.  EXPECTED TO FAIL: TLC's counterexample is the stale
\* splice, a dump verdict placed at a late point resurrecting a state
\* the log had already superseded.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  Keys <- MainKeys
  Vals <- MainVals
  BracketMode = "wrongpoint"
INVARIANTS
  TypeOK
  FrontierExact
CHECK_DEADLOCK FALSE
