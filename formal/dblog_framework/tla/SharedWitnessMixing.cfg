\* Invariant run: the shared witness (Corollary 1) on the two-unit
\* heterogeneous composite.  Unit 2's point is unit 1's witness, so
\* one coordinate is a shared read witness for the whole plan.
\* Expected verdict: every invariant holds, including the trajectory
\* from the shared witness onward and the union cut (Corollary 3).
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 3
  Keys <- MainKeys
  Vals <- MainVals
  Dom1 <- MainDom1
  Dom2 <- MainDom2
  WitnessMode = "shared"
INVARIANTS
  TypeOK
  UnionCut
  SharedTrajectory
  LatestHighTrajectory
CHECK_DEADLOCK FALSE
