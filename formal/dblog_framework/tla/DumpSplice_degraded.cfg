\* Invariant run: the degradation of Theorem 8.  The asserted recovery
\* point is NOT trusted; the bracket widens to every span provably
\* containing the true dump coordinate.  Expected verdict: the cut
\* still holds (FrontierExact green), and the canonical trajectory from
\* the recorded high edge remains (HighEdgeTrajectory green).  The T2
\* frontier result and the T3 exhibition question are separate claims.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 3
  Keys <- MainKeys
  Vals <- MainVals
  BracketMode = "degraded"
INVARIANTS
  TypeOK
  FrontierExact
  HighEdgeTrajectory
CHECK_DEADLOCK FALSE
