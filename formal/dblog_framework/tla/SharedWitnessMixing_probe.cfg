\* PROBE RUN, Proposition 2.  Distinct witnesses, additionally
\* asserting the trajectory claim from the earliest witness.
\* EXPECTED TO FAIL: TLC's counterexample is the tablesync shape, the
\* later unit's verdict honest at its own witness and not source
\* history at coordinates below it.  This probe refutes the stronger
\* earliest-witness start.  It does not refute the safe canonical
\* trajectory from the latest high edge.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  Keys <- MainKeys
  Vals <- MainVals
  Dom1 <- MainDom1
  Dom2 <- MainDom2
  WitnessMode = "distinct"
INVARIANTS
  TypeOK
  DistinctOnsetTrajectory
CHECK_DEADLOCK FALSE
