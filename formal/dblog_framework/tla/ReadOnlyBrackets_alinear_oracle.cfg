\* MUTATION RUN, the A-linear violation probe, oracle facet
\* (model-level audit content; owner-ruled addendum 2026-08-08).
\* BoundarySampling = FALSE keeps every transaction atomically committed
\* but permits a current executed set to be assigned an interior-prefix
\* denotation within its final committed transaction block.  EXPECTED TO
\* FAIL: OracleFaithful; the set-difference membership test then
\* mis-classifies later events in that block against the positional
\* window (Theorem 7's premise is necessary, not merely convenient).
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 2
  BoundarySampling = FALSE
  UncommittedProbe = FALSE
  Keys <- AlinKeys
  Vals <- AlinVals
  ChunkDoms <- AlinChunkDoms
INVARIANTS
  TypeOK
  CommittedOnly
  OracleFaithful
CHECK_DEADLOCK FALSE
