\* MUTATION RUN, Proposition 1, part (1), under write-free brackets.
\* Same configuration as ReadOnlyBrackets_torn_green.cfg, plus the
\* assertion that a single shared in-bracket read boundary exists for
\* every completed chunk.  EXPECTED TO FAIL: the torn read of
\* Proposition 1 is reachable with set-valued watermarks too.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 1
  BoundarySampling = TRUE
  UncommittedProbe = FALSE
  Keys <- TornKeys
  Vals <- TornVals
  ChunkDoms <- TornChunkDoms
INVARIANTS
  TypeOK
  CommittedOnly
  SharedInstantExists
CHECK_DEADLOCK FALSE
