\* Proposition 1, parts (2) and (3), under write-free brackets: one
\* chunk over two keys, up to two single-event transactions, lawful
\* boundary sampling.  Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 1
  BoundarySampling = TRUE
  UncommittedProbe = FALSE
  Keys <- TornKeys
  Vals <- TornVals
  ChunkDoms <- TornChunkDoms
INVARIANTS
  TypeOK
  CommittedOnly
  OracleFaithful
  FrontierExact
  ContinuationExact
  PerKeyMonotone
CHECK_DEADLOCK FALSE
