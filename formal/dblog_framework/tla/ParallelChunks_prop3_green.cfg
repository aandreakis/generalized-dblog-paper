\* Proposition 3, lawful half, on the countermodel configuration: one
\* key, one unit, up to two events, clause (a) at its defined reach.
\* The gated emission replays correctly on the same instance whose
\* narrowed variant fails.  Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  WithholdClauseA = TRUE
  PointBrackets = FALSE
  Keys <- Prop3Keys
  Vals <- Prop3Vals
  ChunkDoms <- Prop3ChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
  ContinuationExact
  GatedMonotone
CHECK_DEADLOCK FALSE
