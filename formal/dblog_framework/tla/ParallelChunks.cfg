\* Invariant run: parallel chunks with gated range-merge on a small,
\* non-trivial configuration (3 keys in 2 concurrent chunks, up to 4
\* data events, deletes included; lawful clause-(a) gate; A-handoff
\* start chosen nondeterministically at or below every high edge).
\* Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 4
  WithholdClauseA = TRUE
  PointBrackets = FALSE
  Keys <- MainKeys
  Vals <- MainVals
  ChunkDoms <- MainChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
  ContinuationExact
  GatedMonotone
CHECK_DEADLOCK FALSE
