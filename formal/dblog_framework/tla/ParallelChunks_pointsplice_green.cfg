\* Proposition 2, in-contract half, on the countermodel configuration:
\* two point-bracket units over disjoint single-key scopes, up to two
\* events.  Each unit's refresh is honest at its own point coordinate;
\* the composite satisfies the contract and its cut and monotonicity
\* hold.  Expected verdict: every invariant holds.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  WithholdClauseA = TRUE
  PointBrackets = TRUE
  Keys <- PointKeys
  Vals <- PointVals
  ChunkDoms <- PointChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
  ContinuationExact
  GatedMonotone
CHECK_DEADLOCK FALSE
