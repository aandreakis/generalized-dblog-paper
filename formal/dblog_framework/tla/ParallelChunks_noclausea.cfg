\* MUTATION RUN, Proposition 3 (withholding is necessary).  Same
\* configuration as ParallelChunks_prop3_green.cfg, with clause (a)
\* narrowed to the window alone (WithholdClauseA = FALSE): the reader
\* absorbs the window into the buffer but streams every other consumed
\* event, pre-bracket events included.  EXPECTED TO FAIL:
\* FrontierExact; a pre-bracket insert of a key deleted inside the
\* window replays to a value the source no longer holds, exactly the
\* paper's two-event construction.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  WithholdClauseA = FALSE
  PointBrackets = FALSE
  Keys <- Prop3Keys
  Vals <- Prop3Vals
  ChunkDoms <- Prop3ChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
CHECK_DEADLOCK FALSE
