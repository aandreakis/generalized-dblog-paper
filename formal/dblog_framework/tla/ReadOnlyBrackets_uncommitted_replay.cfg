\* DELIBERATE OUT-OF-CONTRACT MUTATION, replay facet. This uses the same
\* partial-visibility behavior as ReadOnlyBrackets_uncommitted_oracle.cfg
\* but checks FrontierExact. EXPECTED TO HOLD on the complete small search
\* because ReadKey remains boundary-honest; this is diagnostic robustness,
\* not a guarantee for uncommitted CDC input.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  MaxTxnSize = 2
  BoundarySampling = TRUE
  UncommittedProbe = TRUE
  Keys <- AlinKeys
  Vals <- AlinVals
  ChunkDoms <- AlinChunkDoms
INVARIANTS
  TypeOK
  FrontierExact
CHECK_DEADLOCK FALSE
