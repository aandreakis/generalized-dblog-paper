\* A-LINEAR PROBE, replay facet at the non-trivial size (model-level
\* audit content; owner-ruled addendum 2026-08-08).  The malformed
\* interior-prefix denotation on the main configuration (3 keys in 2 chunks, up to 4
\* events in transactions of up to 2 events): the robustness claim of
\* ReadOnlyBrackets_alinear_replay.cfg searched at the same size as
\* the lawful main run.  EXPECTED TO HOLD: FrontierExact.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 4
  MaxTxnSize = 2
  BoundarySampling = FALSE
  UncommittedProbe = FALSE
  Keys <- MainKeys
  Vals <- MainVals
  ChunkDoms <- MainChunkDoms
INVARIANTS
  TypeOK
  CommittedOnly
  FrontierExact
CHECK_DEADLOCK FALSE
