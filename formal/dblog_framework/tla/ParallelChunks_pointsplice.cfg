\* MUTATION RUN, Proposition 2's constructed distinct-witness fixture.
\* Same configuration as ParallelChunks_pointsplice_green.cfg,
\* plus the assertion that some consistency onset strictly below the
\* frontier exists from which the delivered dataset tracks true source
\* states (Corollary 1's trajectory property).  EXPECTED TO FAIL: with
\* the two point splices at distinct coordinates, the later unit's
\* refresh asserts a value the source did not yet hold at intermediate
\* coordinates, so no onset below the frontier exists in this fixture.
\* The canonical trajectory beginning at the latest high remains.
SPECIFICATION Spec
CONSTANTS
  NULL = NULL
  MaxEvents = 2
  WithholdClauseA = TRUE
  PointBrackets = TRUE
  Keys <- PointKeys
  Vals <- PointVals
  ChunkDoms <- PointChunkDoms
INVARIANTS
  TypeOK
  EarlyOnsetExists
CHECK_DEADLOCK FALSE
